import React from "react";
import { GridProps, ScreenTypes } from "./Grid.interfaces";
export declare const useGridStyles: (props: GridProps) => {
  deviceType: ScreenTypes;
  customStyles: React.CSSProperties;
};
